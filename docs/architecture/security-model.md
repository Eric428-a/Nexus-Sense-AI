# Security Model

Security is separated into authentication, authorization, rate limiting and audit logging. Production deployments should require authenticated identities, least-privilege roles, secret management, TLS and immutable audit retention.
