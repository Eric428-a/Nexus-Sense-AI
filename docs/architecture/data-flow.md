# Data Flow

`Raw Source → SourceDocument → ContentChunk → Entity/Relationship/Event → Evidence → Hypothesis → Finding → IntelligenceReport`

Vector representations support semantic retrieval; the knowledge graph supports relationship-aware traversal.
