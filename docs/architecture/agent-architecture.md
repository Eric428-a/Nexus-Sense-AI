# Agent Architecture

Agents implement a common asynchronous contract. The orchestrator coordinates specialized agents while workflows define business-level sequences. Agent context carries task input, models, memory and metadata.
